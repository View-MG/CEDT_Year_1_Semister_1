#include <bits/stdc++.h>
using namespace std;

int main() {
    int x,y,z;
    cin >> x >> y >> z;

    vector<int> ansa;
    vector<int> ansc;

    for(int i=1 ; i<=x/2 ; i++) if(a%i == 0) ansa.push_back(i);
    for(int i=1 ; i<=z/2 ; i++) if(c%i == 0) ansc.push_back(i);
    if(x!=1) ansa.push_back(x);
    if(z!=1) ansa.push_back(z);

    for(int i=0 ; i<)
}