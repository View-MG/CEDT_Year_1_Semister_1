#include<bits/stdc++.h>
using namespace std;
int main() {
    string a,b,c;
    int b_size = b.size();
    cin >> a >> b >> c;
    string fr(b.size(), '9');
    long long ab = stoi(fr);
    cout << ab;
}