#include<bits/stdc++.h>
using namespace std;
int main(){
    string a,b;
    getline(cin,a);
    getline(cin,b);
    for(int i=0 ; i<b.size()-a.size() ; i++){
        for(int j=0 ; j<a.size() ; j++){
            if(a[i+j] == )
        }
    }
}