#include<bits/stdc++.h>
using namespace std;
int main(){
    double W;
    int n;
    vector<double> v,w,ratio;
    for(int i{0} ; i<n ; i++){
        double a;
        v.push_back(a);
    }
    for(int i{0} ; i<n ; i++){
        double a;
        w.push_back(a);
        ratio.push_back(v[i]/a);
    }

}