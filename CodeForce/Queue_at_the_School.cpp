#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,m;
    cin >> n >> m ;
    vector<char> s(n);
    for(int i=0 ; i>n ; i++){
        cin >> s[i];
    }
    for(auto &i : s){
        if(i==)
    }
    
}