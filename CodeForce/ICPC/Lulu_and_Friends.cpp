#include<bits/stdc++.h>
using namespace std;
int main(){
    string word;
    cin >> word;
    int n;
    cin >> n;
    vector<string> subs(n);
    vector<int> ans();
    for(int i=0 ; i<n ; i++){
        cin >> subs[i];
    }
    for(int i=0 ; i<n ; i++){
        int j=0;
        while(j != word.size())
    }

}